# SPA fallback is handled by wrangler.json `not_found_handling: "single-page-application"`.
# Keeping this file empty avoids the Cloudflare "infinite loop" error (code 100324)
# caused by `/* /index.html 200` (which would re-match /index.html itself).